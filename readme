To create virtual environment: python -m venv venv

To start backend:
  from root directory run:
  pipenv shell
  flask run


to start frontend: npm run dev

CREATE USER thatsawrapautostylin WITH PASSWORD 'password' CREATEDB;
CREATE DATABASE thatsawrapautostylin_db WITH OWNER thatsawrapautostylin;
